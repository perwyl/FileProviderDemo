# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'
use_frameworks!

target 'fileProviderDemo' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  
  pod 'FileProvider'
  pod 'SCLAlertView'

  target 'fileProviderDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'fileProviderDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
